{
	"version": "2.0.0",
	"tasks": [
		{
            "label": "Bun Debug Build",
            "type": "shell",
            "command": "bun run build --sourcemap",
            "options": {
                "cwd": "${workspaceFolder}"
            },
			"group": "build",
        }
	]
}