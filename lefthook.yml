# EXAMPLE USAGE
# Refer for explanation to following link:
# https://github.com/Arkweid/lefthook/blob/master/docs/full_guide.md
#
pre-commit:
  parallel: true
  commands:
    prettier:
      glob: "*.{js,ts}"
      run: yarn pretty-quick --staged

commit-msg:
  parallel: true
  commands:
    conventional-commit:
      run: yarn commitlint --edit
